@model IEnumerable<MVC.Models.Foto>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.img_foto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.img_foto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_foto }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_foto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_foto })
        </td>
    </tr>
}

</table>
